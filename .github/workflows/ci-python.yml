name: CI • Python

on:
  push:
    branches: [main]
    paths:
      - "bindings/python/**"
      - ".github/workflows/ci-python.yml"
  pull_request:
    paths:
      - "bindings/python/**"
      - ".github/workflows/ci-python.yml"
  workflow_dispatch:

defaults:
  run:
    working-directory: bindings/python

jobs:
  test:
    name: lint & test (py${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      # Style gates are advisory until the existing debt is cleaned up
      # (run `ruff check --fix .` and `black .` locally), then drop continue-on-error.
      - name: Ruff (lint)
        run: ruff check .
        continue-on-error: true

      - name: Black (format check)
        run: black --check .
        continue-on-error: true

      - name: Mypy (types)
        run: mypy umicp_sdk
        continue-on-error: true

      - name: Pytest
        run: pytest
